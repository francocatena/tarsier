<div class="row">
  <div class="span10 offset3" id="new_password_container">
    <h2>Cambio de contraseña</h2>

    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }, wrapper: :inline) do |f| %>
      <%= devise_error_messages! %>
      <%= f.input :reset_password_token, as: :hidden %>

      <%= f.input :password, label: 'Nueva contraseña' %>
      <%= f.input :password_confirmation, label: 'Confirmar contraseña' %>

      <%= f.button :submit, 'Cambiar mi contraseña', class: 'primary' %>
    <% end %>

    <%= render partial: 'devise/shared/links' %>
  </div>
</div>